/*
 * SEO Management Composable
 * This composable should contain:
 * - Dynamic meta tag management
 * - Open Graph tags for social sharing
 * - Twitter Card meta tags
 * - Structured data (JSON-LD) generation
 * - Canonical URL management
 * 
 * Functions to export:
 * - useSEO() - main composable function
 * - setPageMeta(meta) - set page-specific meta tags
 * - setOpenGraph(og) - set Open Graph tags
 * - setTwitterCard(twitter) - set Twitter Card tags
 * - setStructuredData(data) - set JSON-LD structured data
 * 
 * Meta object structure:
 * - title, description, keywords, author, canonical
 * - ogTitle, ogDescription, ogImage, ogType
 * - twitterTitle, twitterDescription, twitterImage
 */
